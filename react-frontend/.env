# Leave empty: browser calls /api/... on the Vite dev server; Vite proxies to http://localhost:4000 (vite.config.js).
# For production build, set full API URL, e.g. VITE_API_URL=https://api.example.com
VITE_API_URL=http://localhost:4000
