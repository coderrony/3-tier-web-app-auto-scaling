# Leave empty to use the Vite dev proxy (see vite.config.js) — requests go to /api → backend.
# Or set the full API origin, e.g.:
VITE_API_URL=http://localhost:4000
# Client-side only if prefixed with VITE_
VITE_ENV=development
